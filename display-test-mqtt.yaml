esphome:
  name: display-c3
  friendly_name: display-c3

esp32:
  board: esp32-c3-devkitm-1
  variant: esp32c3
  framework:
    type: arduino

external_components:
  - source: 
      type: local
      path: ./components
    components: [waveshare_epaper]

logger:
  level: debug

api:
  encryption:
    key: "HhpDctFSVQFcpkPbjk5ljZ3ux3IKbAY8/fft3AoJJ40="

ota:
  - platform: esphome
    password: "26cb5bd41399195a2429ea324e4139f9"

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  fast_connect: True

  ap:
    ssid: "Display-Test Fallback Hotspot"
    password: "LZ7rZuNsLytx"  


debug:
  update_interval: 5s

text_sensor:
  - platform: debug
    device:
      name: "Device Info"
    reset_reason:
      name: "Reset Reason"

sensor:
  - platform: sht3xd
    temperature:
      id: LRTemp
      name: "Living Room Temperature"
    humidity:
      id: LRRH
      name: "Living Room Humidity"
    address: 0x44
    update_interval: 10s
  - platform: homeassistant
    entity_id: sensor.limmat_temperature
    id: limmat_temperature
  - platform: debug
    free:
      name: "Heap Free"
    block:
      name: "Heap Max Block"
    loop_time:
      name: "Loop Time"
    psram:
      name: "Free PSRAM"


psram:



# MQTT configuration for subscribing to sleep duration, OTA, and calendar events
mqtt:
  discovery: true  # Enable MQTT discovery
  broker: !secret mqtt_host  # MQTT broker address stored in secrets
  username: !secret mqtt_user  # MQTT username stored in secrets
  password: !secret mqtt_password  # MQTT password stored in secrets

  on_message:
    # Subscribe to calendar event updates
    - topic: "homeassistant/calendar/event"
      then:
        - lambda: |-
            std::string agenda_json = x.c_str();
            ESP_LOGD("agenda", "Received JSON: %s", agenda_json.c_str());
            
            // Cache the received JSON
            id(cached_agenda_json) = agenda_json;

            const size_t capacity = JSON_OBJECT_SIZE(10) + JSON_ARRAY_SIZE(10) + 10 * JSON_OBJECT_SIZE(4);
            StaticJsonDocument<capacity> doc;

            DeserializationError error = deserializeJson(doc, agenda_json);
            if (error) {
              ESP_LOGE("agenda", "Failed to parse JSON: %s", agenda_json.c_str());
              return;
            }

            // Update the display if parsing was successful
            id(waveshare_display).update();

# Global variables for dynamic sleep duration and cached agenda JSON
globals:

  - id: cached_agenda_json  # Variable to cache the last received calendar events
    type: std::string
    restore_value: yes
    initial_value: ""


spi:
  clk_pin: GPIO7
  mosi_pin: GPIO21  # MOSI is required
  
spi_device:
  - bit_order: msb_first
    data_rate: 10MHz
    spi_mode: 0

font:
  - file: "gfonts://Roboto"
    id: roboto_10
    size: 30
  - file: "gfonts://Orbitron"
    id: Orbitron_30
    size: 40
  - file: "gfonts://Orbitron"
    id: Orbitron_15
    size: 15
  - file: "gfonts://Orbitron"
    id: Orbitron_BIG
    size: 100
  - file: "gfonts://Audiowide"
    id: Audiowide_BIG
    size: 80
  - file: "gfonts://Audiowide"
    id: Audiowide_small
    size: 25
  - file: "gfonts://Audiowide"
    id: Audiowide_32
    size: 35
  - file: "gfonts://Audiowide"
    id: Audiowide_med
    size: 45

display:
  - platform: waveshare_epaper
    model: gdep073e01
    cs_pin: GPIO5
    dc_pin: GPIO4
    reset_pin: GPIO3
    busy_pin: GPIO6  # Input pin
    update_interval: 1200s #1800s
    rotation: 270
    lambda: |-
      auto black = Color(0x00, 0x00, 0x00);
      auto red = Color(0xFF, 0x00, 0x00);
      auto green = Color(0x00, 0xFF, 0x00);
      auto blue = Color(0x00, 0x00, 0xFF);
      auto white = Color(0xFF, 0xFF, 0xFF);
      auto yellow = Color(0xFF, 0xFF, 0x00);

      it.fill(green);
      it.filled_rectangle(it.get_width()/2+6, 3, it.get_width()-6, 160 , white);
      it.printf(it.get_width()/2+20, 8, id(Audiowide_med), black, "LIMMAT");  
      it.printf(it.get_width()/2+20, 50, id(Audiowide_BIG),black, "%.1f°", id(limmat_temperature).state);  
      
      it.filled_rectangle(it.get_width()/2+6, 170, it.get_width()-6, 100 , white);
      it.printf(it.get_width()/2+20, 175, id(Audiowide_small), black, "LIVING ROOM" );  
      it.printf(it.get_width()/2+20, 205, id(Audiowide_32), black, "%.1f°  %.0f%%", id(LRTemp).state, id(LRRH).state);  

      it.filled_rectangle(it.get_width()/2+6, 280, it.get_width()-6, 100 , white);
      // it.printf(it.get_width()/2+20, 205, id(Audiowide_32), black, "%.1f°  %.0f%%", id(events)[0].start_time; 


      it.filled_rectangle(3, 3, it.get_width()/2-6, it.get_height()-6, white);
      for (int i = 0; i < 14; i++) {
        int line_pos = (i * (it.get_height()-6) / 13)+3; 
        it.line(3, line_pos+1, it.get_width()/2-6, line_pos+1, green);
        if (i%2==0)
          it.printf(5, line_pos+2, id(Audiowide_small),black, "%d", i+7);      
        }

      int y_pos = 390;
      for (JsonObject event : day_pair.value().as<JsonArray>()) {
            const char* event_summary = event["summary"] | "No Title";
            std::string event_start = event["start"].as<std::string>();
            std::string event_end = event["end"].as<std::string>();
            it.printf(it.get_width()/2+20, y_pos, id(Audiowide_small), black, "%s", event_summary);
            y_pos += 30;
      }













i2c:
  sda: GPIO8
  scl: GPIO20
  scan: true
  frequency: 50khz
  


captive_portal:    
    
    